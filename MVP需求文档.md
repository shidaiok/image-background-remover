# Image Background Remover MVP 需求文档

## 1. 项目概述

### 1.1 产品名称
Image Background Remover

### 1.2 一句话定义
一个基于 Cloudflare 部署的在线图片去背景工具，用户上传图片后即可获得透明背景结果并下载。

### 1.3 目标
- 让用户在最少操作步骤内完成抠图
- 以低成本验证“在线去背景”这个需求是否成立
- 保持架构简单，无数据库、无持久化存储、无账号体系

## 2. 产品边界

### 2.1 MVP 只做
- 上传单张图片
- 调用 Remove.bg API 完成背景移除
- 展示处理结果预览
- 下载透明背景 PNG
- 展示失败原因和基础错误提示

### 2.2 MVP 不做
- 用户登录 / 注册
- 历史记录
- 批量处理
- 图片编辑器
- 背景替换
- 云端存储
- 任务队列
- 订阅 / 计费
- 图片分享链接

## 3. 目标用户
- 需要快速抠图的个人用户
- 轻量内容创作者
- 需要在网页里快速处理商品图、头像图、素材图的用户

## 4. 用户场景
1. 用户打开页面
2. 选择或拖入一张图片
3. 点击开始处理
4. 页面等待结果返回
5. 展示原图和处理结果
6. 用户下载 PNG 文件

## 5. 功能需求

### 5.1 图片上传
- 支持拖拽上传和点击选择文件
- 仅支持单张图片
- 接受常见图片格式，如 JPG、PNG、WEBP
- 上传前做基础校验：文件类型、文件大小
- 文件不落盘，不入库，仅在请求生命周期内传递

### 5.2 图片处理
- 前端将图片发送到 Cloudflare Worker / Pages Function
- 服务端调用 Remove.bg API 完成背景移除
- 服务端将结果直接返回给前端
- 全流程不保存原图和结果图

### 5.3 结果展示
- 显示处理后的透明背景图片
- 提供原图与结果图的对比区域
- 支持在浅色和深色背景下查看结果，便于检查边缘效果

### 5.4 下载
- 提供下载按钮
- 默认下载格式为 PNG
- 文件名使用原文件名加后缀或默认命名

### 5.5 错误处理
- 图片格式不支持时提示用户重新上传
- 文件过大时提示用户压缩后重试
- Remove.bg 返回错误时展示通用失败提示，并保留可读错误信息
- 网络超时或请求失败时提示重新尝试

## 6. 非功能需求

### 6.1 性能
- 单张图片处理链路尽量短
- 页面首屏加载轻量
- 处理状态必须有明确反馈

### 6.2 安全
- Remove.bg API Key 只放在 Cloudflare 环境变量或 Secret 中
- 前端不暴露密钥
- 不保存用户图片

### 6.3 可用性
- 页面结构简单，用户 3 步内完成任务：上传、处理、下载
- 移动端可用

### 6.4 约束
- 依赖 Remove.bg API 的可用性和限制
- 依赖 Cloudflare 平台请求体和运行限制
- 只支持在线处理，不提供离线模式

## 7. 技术方案

### 7.1 部署
- 前端：Cloudflare Pages
- 接口：Cloudflare Worker 或 Pages Function
- 外部抠图服务：Remove.bg API

### 7.2 数据流
1. 用户上传图片
2. 前端把图片发给 Worker
3. Worker 转发到 Remove.bg
4. Remove.bg 返回处理后的图片
5. Worker 将结果回传给前端
6. 前端展示并允许下载

### 7.3 存储策略
- 不使用数据库
- 不使用对象存储
- 不写入本地磁盘
- 所有图片只在内存和请求链路中临时存在

## 8. 验收标准

### 8.1 功能验收
- 用户能成功上传图片
- 系统能返回去背景后的图片
- 用户能下载结果 PNG
- 异常场景有明确提示

### 8.2 体验验收
- 用户不需要登录即可使用
- 页面操作路径清晰
- 处理状态可见，不会让用户误以为页面卡死

### 8.3 技术验收
- API Key 不暴露在前端代码中
- 不产生图片持久化存储
- 所有请求都可在 Cloudflare 上完成部署和运行

## 9. 风险与待确认事项

### 9.1 风险
- Remove.bg 的请求限制或费用变化
- 超大图片在平台限制下失败
- 不同图片质量导致抠图边缘效果不稳定

### 9.2 待确认
- 是否需要限制最大上传尺寸
- 是否需要支持批量上传，当前 MVP 不建议做
- 是否要提供白底 / 黑底预览切换，建议作为可选增强项

## 10. MVP 成功标准
- 用户能在一个页面完成上传、抠图、下载
- 无账号、无存储、无复杂配置
- 能稳定验证这个产品是否有使用需求
